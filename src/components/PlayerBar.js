import React, {Component} from 'react';

class PlayerBar extends Component {
  render() {
    return(
      <section>
        A player bar goes here
      </section>
    );
  }
}

export default PlayerBar;
